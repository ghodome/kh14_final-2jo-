<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="auction">
	<insert id="add">
	insert into auction(
		auction_low_price,auction_high_price,
		auction_start_date,auction_end_date,
		auction_state
	)
	values(
		#{auctionLowPrice},#{auctionHighPrice},
		#{auctionStartDate},#{auctionEndDate},
		#{auctionState}
	)
	</insert>

	<update id="update">
		update auction set 
		auction_low_price=#{auctionLowPrice}, auction_high_price=#{auctionHighPrice},
		auction_start_date=#{auctionStartDate},auction_end_date=#{auctionEndDate},
		auction_state=#{auctionState} where auction_no=#{auctionNo}
	</update>
	
	<select id="list" resultType="auctionDto">
		select * from auction order by auction_no asc
	</select>
	<select id="detail" resultType="auctionDto">
		select * from auction where auction_no=#{auctionNo}
	</select>
	<delete id="delete">
		delete auction where auction_no=#{auctionNo}
	</delete>
	<select id="sceduleList" resultType="auctionDto">
		select * from auction where auction_schedule_no = #{auctionScheduleNo} order by auction_no asc
	</select>
</mapper>