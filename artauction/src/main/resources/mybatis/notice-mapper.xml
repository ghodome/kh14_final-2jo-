<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0 //EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="notice">
    
    <!-- 시퀀스 생성 -->
    <select id="sequence" resultType="int">
        select notice_seq.nextval from dual
    </select>

	<!-- 등록 -->
    <insert id="registration">
        insert into notice
        (
            notice_no, notice_member_id,
            notice_type, notice_title,
            notice_content, notice_wtime,
            notice_views
        )
        values
        (
            #{noticeNo}, #{noticeMemberId},
            #{noticeType}, #{noticeTitle},
            #{noticeContent}, #{noticeWtime},
            #{noticeViews}
        )
    </insert>

    <!-- 목록 -->
    <select id="list" resultType="NoticeDto">
        select * from notice order by notice_no asc
    </select>
    
    <!-- 상세 -->
    <select id="detail" resultType="NoticeDto">
    	select * from notice where notice_no = #{noticeNo}
    </select>
</mapper>